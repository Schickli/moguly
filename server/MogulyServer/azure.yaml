# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: moguly-server
services:  
  app:
    language: dotnet
    project: ./MogulyServer.AppHost/MogulyServer.AppHost.csproj
    host: containerapp
